

Bump the version in `package.json` from the current value to the next patch version, and ensure the sidebar displays it.

**Change:** Update `package.json` version field (e.g., `1.2.0` → `1.2.1`).

